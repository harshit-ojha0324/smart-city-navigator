-r requirements.txt
pytest>=8.0,<10
pytest-asyncio>=0.23,<2
ruff>=0.6,<0.17              # lint rules change between minors; a bump is a
                             # deliberate upgrade, not a surprise red build
